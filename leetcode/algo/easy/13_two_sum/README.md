# Two numbers

Given the array of number, need to find two distinct index that sum == target

[url](https://leetcode.com/problems/two-sum/description/)

Solutions:
1. `twoSum` -- using map for storing val and indexes, O(N) -- CPU, O(N) -- RAM


tags:
`Array`, `Hash Table`