package j

func main() {
	// идея в том, чтобы при создании каждого нового view ссылаться сразу
	// на оригинальный массив (как в го --> индекс старта + длина)
	// потому что иначе если ссылаться будут view друг на друга, то придется делать
	// рекурсию, чтобы везде эти значения обновить
}
